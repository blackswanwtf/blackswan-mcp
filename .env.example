# Risk Engine
RISK_ENGINE_URL=                  # Risk Engine base URL (required)
RISK_ENGINE_API_KEY=              # X-API-Key for Risk Engine /api/* routes

# Transport
# TRANSPORT_MODE=stdio  # "stdio" (default) or "http"
# PORT=3000             # HTTP server port (default: 3000, Docker: 8080)

# Logging
LOG_LEVEL=info
